{
  "name": "molochclonegraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ odyssy-automaton/pillar-ambassador-temp",
    "create-local": "graph create --node http://localhost:8020/ odyssy-automaton/pillar-ambassador-temp",
    "remove-local": "graph remove --node http://localhost:8020/ odyssy-automaton/pillar-ambassador-temp",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 odyssy-automaton/pillar-ambassador-temp"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.15.0",
    "@graphprotocol/graph-ts": "0.15.0"
  }
}
